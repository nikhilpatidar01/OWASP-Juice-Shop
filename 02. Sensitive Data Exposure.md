# Sensitive Data Exposure Total 19 Hacking Challenges
Sensitive Data Exposure occurs when an application improperly protects sensitive information such as passwords, credit card numbers, personal data, or API keys. Attackers can exploit this to steal data or impersonate users. Proper handling includes encryption, secure storage, and limited exposure.

**Examples in OWASP Juice Shop:**

* Exposed API keys or passwords in source code
* Unencrypted data in local storage or cookies
* Visible sensitive information in error messages or responses

**Tips to Solve:**

* Inspect the **frontend source code**, browser **local storage**, and **cookies**.
* Check **network requests** in developer tools for exposed data.
* Look for hidden fields, debug info, or configuration files revealing sensitive information.

---
# 1. Confidential Document

> The Confidential Document vulnerability occurs when sensitive files or directories (such as internal documents, backups, or configuration files) are left exposed on the server without proper access control. Attackers can discover and access these files directly, leading to information disclosure.

---

**Access and Solve:**

### Method 1: Directory Fuzzing (ffuf)

> Run a directory brute-force scan to identify hidden resources:

```bash
ffuf -w /usr/share/seclists/Discovery/Web-Content/directory-list-lowercase-2.3-medium.txt -u http://10.30.30.145:3000/FUZZ
```

> Check for status **200 OK** responses. Example discovery of the `/ftp` directory:

```bash
┌──(root㉿Webpentest)-[/usr]
└─# ffuf -w /usr/share/seclists/Discovery/Web-Content/directory-list-lowercase-2.3-medium.txt -u http://10.30.30.145:3000/FUZZ | grep ftp

ftp                     [Status: 200, Size: 12464, Words: 1596, Lines: 362, Duration: 1502ms]
:: Progress: [3877/207643] :: Job [1/1] :: 4 req/sec :: Duration: [0:02:46] :: Errors: 0 ::^C
```

### Method 2: Burp Suite

1. Intercept traffic with Burp Suite.
2. Analyze responses for hidden directories.
3. Identify the endpoint:

```
/ftp/acquisitions.md
```

**Direct Access:**
Navigate directly in the browser to retrieve the file:

```
http://10.30.30.145:3000/ftp/acquisitions.md
```

**Result:**
> By accessing the hidden `/ftp/acquisitions.md` file, the confidential document is disclosed and the challenge is completed.

---
# 2. Exposed Metrics

> The Exposed Metrics challenge requires you to locate an endpoint that provides application usage data intended for monitoring systems. When improperly exposed, this endpoint can reveal internal operational details, which attackers could leverage.

**Steps to Access and Solve:**

1. Open the application in your browser.
2. Navigate to or directly request the **metrics endpoint**:

   ```html
   http://10.30.30.145:3000/metrics
   ```
3. Inspect the returned data for system metrics like request counts, error rates, or other internal statistics.
4. If needed, use hints from **Prometheus documentation** for understanding metrics format:
   [Prometheus First Steps](https://prometheus.io/docs/introduction/first_steps/)

**Vulnerable Parameter:**

* None required; the endpoint itself is publicly accessible.

**Tip:**

* Metrics are usually plain text and can be scraped by monitoring tools.
* Accessing this endpoint correctly in Juice Shop will mark the challenge as solved.

---

# 3. Login MC SafeSearch

> Log in with MC SafeSearch's original user credentials without using SQL Injection or any bypass.

* **Task:** Authenticate using the legitimate credentials of MC SafeSearch.

**Steps to Access and Solve:**

1. Read the challenge hints or search online for "MC SafeSearch" to find the music video ["Protect Ya' Passwordz"](https://www.youtube.com/watch?v=v59CX2DiX0Y)
2. Watch the video to learn that MC used his dog’s name, **"Mr. Noodles"**, as a password, with some vowels replaced by zeros.
3. Visit the login page:

   ```
   http://10.30.30.145:3000/#/login
   ```
4. Log in with the following credentials:

   ```text
   Email: mc.safesearch@juice-sh.op
   Password: Mr. N00dles
   ```

* **✅ Task Completed:** Successfully logged in as MC SafeSearch.

---


Here’s your content **cleaned, properly formatted, and shortened** while keeping all key steps:

---

# 4. Meta Geo Stalking
> Meta Geo Stalking is tracking or identifying a person’s location and movements using metadata embedded in digital content, such as photos, videos, or social media posts. Metadata often includes **GPS coordinates, timestamps, and device info**, revealing sensitive details without consent.

* **Task:** Determine John’s security question answer from a photo on the Photo Wall and use it to **reset his password** via the Forgot Password feature.

**Steps to Solve:**
1. Visit the Photo Wall and locate the photo posted by user `j0hNny`.
2. Download the photo and check its metadata (e.g., via [Ezgif](https://ezgif.com/view-metadata)).
3. Extract coordinates: `36.958717N, 84.348217W`.
4. Search the coordinates on Google → **Daniel Boone National Forest**.
5. Go to the login page → Forgot your password?
6. Enter `john@juice-sh.op` and use **Daniel Boone National Forest** as the security answer.
7. Set a new password and submit.

```php
Email - john@juice-sh.op
Security Answer - Daniel Boone National Forest
```

* **✅ Task Completed:** Successfully determined John’s security answer from photo metadata and reset his password.
---
